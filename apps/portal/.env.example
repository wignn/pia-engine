# Base API path used by the frontend in any mode.
# For local dev with Vite proxy, keep this as /api/v1.
VITE_API_BASE=/api/v1

# Dev server proxy target (used by vite.config.js)
# Change if control-plane runs on a different host/port.
VITE_DEV_PROXY_TARGET=http://localhost:8080
