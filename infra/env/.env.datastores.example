# Shared datastore credentials (clickhouse + redis + services that connect).
# Copy to .env.datastores and mirror REDIS_PASSWORD into each service env:
#   REDIS_URL=redis://:<REDIS_PASSWORD>@redis:6379
CLICKHOUSE_PASSWORD=replace-with-random-secret
REDIS_PASSWORD=replace-with-random-secret
